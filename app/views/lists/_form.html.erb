<div class="container col-6">
  <%= simple_form_for(list) do |f| %>
    <%= f.input :name, placeholder: "Enter the name of your list"%>
    <%= f.button :submit, class: "btn btn-primary" %>
  <% end %>
</div>

